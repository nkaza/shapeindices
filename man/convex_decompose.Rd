% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convex-decompose.R
\name{convex_decompose}
\alias{convex_decompose}
\title{Convex decomposition of a (multi)polygon (Hertel-Mehlhorn).}
\usage{
convex_decompose(poly, prep = NULL, simplify_tolerance = NULL)
}
\arguments{
\item{poly}{a single sfg/sfc (MULTI)POLYGON}

\item{prep}{optional pre-computed list(poly, tri) from prepare_polygon(),
to skip re-triangulating}

\item{simplify_tolerance}{passed to \code{prepare_polygon()} when \code{prep} is
NULL - see its own doc. Ignored (with no error) if \code{prep} is supplied
directly, since simplification only happens while building \code{prep} in
the first place.}
}
\value{
an sf data frame shaped like cdt_triangles()'s output (piece_id,
area, geometry), or NULL if the polygon triangulates to no pieces
}
\description{
Not currently called by any of this package's own index functions -
every index works directly off \code{cdt_triangles()}'s triangles (optionally
refined finer by \code{subdivide_mesh()}), never off merged convex pieces.
This is a standalone utility for a caller who wants an approximate
convex decomposition for their own purposes; see \code{subdivide_mesh()} for
the opposite direction (more, smaller triangles rather than fewer,
larger convex pieces).
}
\examples{
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
tri    <- cdt_triangles(nc[nc$NAME == "Dare", ])
pieces <- convex_decompose(nc[nc$NAME == "Dare", ])
nrow(tri)     # many small triangles
nrow(pieces)  # fewer, larger convex pieces
}
