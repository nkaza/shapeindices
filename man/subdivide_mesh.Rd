% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subdivide-mesh.R
\name{subdivide_mesh}
\alias{subdivide_mesh}
\title{Adaptive subdivision of a CDT mesh into a finer triangle mesh.}
\usage{
subdivide_mesh(poly, prep = NULL, max_depth = 4, simplify_tolerance = NULL)
}
\arguments{
\item{poly}{a single sfg/sfc (MULTI)POLYGON}

\item{prep}{optional pre-computed list(poly, tri) from prepare_polygon(),
to skip re-triangulating}

\item{max_depth}{subdivision depth ceiling, applied to the mesh's own
largest triangle only (4^max_depth children for that triangle; fewer
for smaller ones, adaptively - see \code{.adaptive_tri_depth()})}

\item{simplify_tolerance}{passed to \code{prepare_polygon()} when \code{prep} is
NULL - see its own doc. Ignored (with no error) if \code{prep} is supplied
directly, since simplification only happens while building \code{prep} in
the first place.}
}
\value{
an sf data frame shaped like \code{cdt_triangles()}'s output (tri_id,
area, geometry), or NULL if the polygon triangulates to no pieces
}
\description{
Standalone utility, not currently called by any of this package's own
index functions - \code{radial_concentration_index()} does the same
area-adaptive subdivision internally, but collapses each sub-triangle
straight to a weighted centroid rather than keeping triangle geometries
around, so it doesn't go through this function. See \code{convex_decompose()}
for the opposite direction (fewer, larger convex pieces rather than
more, smaller triangles).
}
\examples{
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
tri  <- cdt_triangles(nc[nc$NAME == "Dare", ])
fine <- subdivide_mesh(nc[nc$NAME == "Dare", ])
nrow(tri)    # coarse CDT triangles
nrow(fine)   # more, smaller triangles
}
