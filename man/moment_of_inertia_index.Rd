% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moment-of-inertia.R
\name{moment_of_inertia_index}
\alias{moment_of_inertia_index}
\title{Polar moment-of-inertia compactness/dispersal index of a (multi)polygon.}
\usage{
moment_of_inertia_index(
  poly,
  prep = NULL,
  weight = NULL,
  simplify_tolerance = NULL
)
}
\arguments{
\item{poly}{a single sfg/sfc (MULTI)POLYGON}

\item{prep}{optional pre-computed list(poly, tri) from prepare_polygon(),
to skip re-triangulating}

\item{weight}{optional numeric vector, one entry per triangle in
\code{prep$tri}, used as that triangle's mass instead of its own area.
NULL (default) reproduces the unweighted index exactly. See the
concentric-rings note above for why the reference changes once
density is non-uniform.}

\item{simplify_tolerance}{passed to \code{prepare_polygon()} when \code{prep} is
NULL - see its own doc. Ignored (with no error) if \code{prep} is supplied
directly, since simplification only happens while building \code{prep} in
the first place.}
}
\value{
list(index, J, Ixx, Iyy, Ixy, J_ref, area, total_weight,
centroid, triangles). \code{centroid} is the plain geometric centroid when
weight = NULL, or the true mass centroid when weighted - J/Ixx/Iyy/Ixy
are always computed about this point (\code{Ixy}, the product of inertia,
isn't used by this index at all - it's here for
\code{moment_isotropy_index()}, which shares this same triangulation and mass
centroid). \code{total_weight} is sum(weight) as supplied (before
normalisation), or area when weight = NULL.
}
\description{
Polar moment-of-inertia compactness/dispersal index of a (multi)polygon.
}
\examples{
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
wake <- nc[nc$NAME == "Wake", ]
moment_of_inertia_index(wake)$index
# Camden: a long, narrow county - convex, but far from a disk
moment_of_inertia_index(nc[nc$NAME == "Camden", ])$index

# weight: substitutes for each triangle's own mass - weighting by the
# triangle's own area exactly reproduces the unweighted index
prep <- prepare_polygon(wake)
moment_of_inertia_index(wake, prep = prep, weight = prep$tri$area)$index
}
