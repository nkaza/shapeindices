% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangulation.R
\name{cdt_triangles}
\alias{cdt_triangles}
\title{Constrained Delaunay triangulation of a (multi)polygon}
\usage{
cdt_triangles(poly)
}
\arguments{
\item{poly}{a single sfg/sfc (MULTI)POLYGON (length 1); does not run
st_make_valid() itself - callers are expected to have cleaned it up}
}
\value{
an sf data frame with one row per triangle (tri_id, area,
geometry), or NULL if the polygon has no triangulatable area
}
\description{
Triangulates each part separately so disjoint parts never bridge, and
hole boundaries are always respected as constraints.
}
\examples{
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
tri <- cdt_triangles(nc[nc$NAME == "Wake", ])
nrow(tri)
plot(sf::st_geometry(tri), border = "grey40")
}
