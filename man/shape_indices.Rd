% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape-indices.R
\name{shape_indices}
\alias{shape_indices}
\title{All indices (or a chosen subset) for a single (multi)polygon}
\usage{
shape_indices(
  poly,
  which = "all",
  deterministic_max_tri = NULL,
  simplify_tolerance = NULL,
  ...
)
}
\arguments{
\item{poly}{a single sfg/sfc (MULTI)POLYGON}

\item{which}{"all" (default), or a character vector naming a subset of
these thirteen values - each listed here with the function it actually
calls, since the \code{which} string and the function name aren't always
identical:
\itemize{
\item \code{"convexity"} - \code{\link[=convexity_index]{convexity_index()}}
\item \code{"moment_of_inertia"} - \code{\link[=moment_of_inertia_index]{moment_of_inertia_index()}}
\item \code{"moment_isotropy"} - \code{\link[=moment_isotropy_index]{moment_isotropy_index()}}
\item \code{"directional_balance"} - \code{\link[=directional_balance_index]{directional_balance_index()}}
\item \code{"span"} - \code{\link[=span_index]{span_index()}}
\item \code{"radial_concentration"} - \code{\link[=radial_concentration_index]{radial_concentration_index()}}
\item \code{"depth"} - \code{\link[=depth_index]{depth_index()}}
\item \code{"hull_ratio"} - \code{\link[=hull_ratio_index]{hull_ratio_index()}}
\item \code{"polsby_popper"} - \code{\link[=polsby_popper_index]{polsby_popper_index()}}
\item \code{"width_length_ratio"} - \code{\link[=width_length_ratio_index]{width_length_ratio_index()}}
\item \code{"reock"} - \code{\link[=reock_index]{reock_index()}}
\item \code{"detour"} - \code{\link[=detour_index]{detour_index()}}
\item \code{"exchange"} - \code{\link[=exchange_index]{exchange_index()}}
}

The first seven need a CDT mesh; the last six are classic
boundary/hull/bounding-box metrics that don't - requesting only those
six skips triangulation entirely. An unrecognised name in \code{which}
errors immediately, listing all thirteen valid values.}

\item{deterministic_max_tri}{if set, meshes with more triangles than
this use \code{deterministic = FALSE} automatically - real-world polygons
can triangulate into thousands of CDT triangles, where the
exhaustive/full-subdivision methods are minutes-to-hours per polygon.
Can only push \code{deterministic} from TRUE/default to FALSE, never
override an explicit \code{deterministic = FALSE} back to TRUE. Shared by
convexity_index()/span_index()/radial_concentration_index()/
directional_balance_index()/depth_index() alike; has no effect on the
six classic metrics, which have no mesh to switch at all.}

\item{simplify_tolerance}{passed to \code{prepare_polygon()} - see its own
doc. Has no effect when \code{which} requests only the six classic
metrics, since no mesh is built for those to begin with.}

\item{...}{passed to convexity_index(), span_index(),
radial_concentration_index(), directional_balance_index(), and
depth_index() (e.g. \code{weight} is shared by all five, and also
forwarded to moment_of_inertia_index()/moment_isotropy_index(), so
every weighted index stays consistently weighted;
\code{deterministic}/\code{n_lines} are shared by all five mesh-based Monte
Carlo indices - \code{n_lines} sets every one of their sample counts with
one argument, even though
span_index()/radial_concentration_index()/directional_balance_index()/
depth_index() never actually build line geometry; \code{n_quad} is
convexity_index()/span_index() only, since the other three mesh
indices' deterministic modes have no quadrature refinement of that
kind to control).}
}
\value{
named numeric vector, one entry per requested index, in
canonical order
}
\description{
Shares one triangulation when any CDT-based index is requested.
}
\examples{
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
wake <- nc[nc$NAME == "Wake", ]
shape_indices(wake)

# a subset - skips triangulation entirely, since none of these need a mesh
shape_indices(wake, which = c("hull_ratio", "polsby_popper", "reock"))

# deterministic_max_tri forces the cheaper Monte Carlo estimator once
# the mesh exceeds it, regardless of how simple the polygon actually is -
# here set deliberately low (5) just to demonstrate the switch on a
# small county
shape_indices(wake, deterministic_max_tri = 5, n_lines = 2000, seed = 1)

# simplify_tolerance trades a small amount of boundary detail (here 50m)
# for a smaller mesh - passed straight through to prepare_polygon()
shape_indices(wake, simplify_tolerance = 50)
}
